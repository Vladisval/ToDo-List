import { Box, Typography } from "@mui/material";

const TaskItem = () => {
  return (
    <Box height='4rem' minWidth='40rem'>
      <Typography variant="body2" color="textSecondary">
          Go to bad!
      </Typography>
    </Box>
  );
};

export default TaskItem;